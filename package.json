{
  "name": "@openai/codex-plugin-cc",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "description": "Use Codex from Claude Code to review code or delegate tasks.",
  "license": "Apache-2.0",
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "bump-version": "bun scripts/bump-version.ts",
    "check-version": "bun scripts/bump-version.ts --check",
    "generate:types": "mkdir -p plugins/codex/.generated/app-server-types && codex app-server generate-ts --out plugins/codex/.generated/app-server-types",
    "typecheck": "bunx tsc",
    "build": "bun run generate:types && bun run typecheck",
    "test": "bun test",
    "format": "bunx --bun @biomejs/biome format --write",
    "lint": "bunx --bun @biomejs/biome lint --write",
    "check": "bunx --bun @biomejs/biome check --write",
    "check:ci": "bunx --bun @biomejs/biome ci ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
